instructions for the moderator:

1. please ensure the app.config files are edited to reflect the directory of the input and output files
located on your own computer, or where the application resides.

2. there are two app.config files to edit
2.1 one app.config file is in the project "BidMain".
2.2 the other app.config file is is the project "BidWeb".

3. clear the output file to test the console app, save the file, and run "BidMain".

4. view the output file, ensure it has the necessary json content.

5. then run "BidWeb" to view the report.